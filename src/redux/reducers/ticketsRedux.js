/* selectors */


/* action name creators */
const reducerName = 'tickets';
const createActionName = name => `app/${reducerName}/${name}`;

/* action types */

/* action creators */


const ticketsReducer = (statePart = {}, action = {}) => {
  switch (action.type) {
    default: 
    return statePart;
  }
};

export default ticketsReducer;